<launch>

  <rosparam command="load" file="$(find sct_gazebo)/config/control.yaml" />

  <node name="controller_spawner" pkg="controller_manager" type="spawner"
        args="sct_joint_publisher sct_chassis_controller" />

</launch>